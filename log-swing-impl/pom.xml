<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<parent>
		<artifactId>impl-parent</artifactId>
		<groupId>org.cytoscape</groupId>
		<version>3.10.5</version>
	</parent>

	<properties>
		<bundle.symbolicName>org.cytoscape.log-swing-impl</bundle.symbolicName>
		<bundle.namespace>org.cytoscape.log.internal</bundle.namespace>
	</properties>

	<modelVersion>4.0.0</modelVersion>
	<groupId>org.cytoscape</groupId>
	<artifactId>log-swing-impl</artifactId>
  <version>3.10.5</version>

	<name>Cytoscape Log Swing Impl (${project.artifactId})</name>

	<packaging>bundle</packaging>


	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<version>${maven-bundle-plugin.version}</version>
				<extensions>true</extensions>
				<configuration>
					<instructions>
						<Bundle-SymbolicName>${bundle.symbolicName}</Bundle-SymbolicName>
						<Bundle-Version>${project.version}</Bundle-Version>
						<Export-Package>!${bundle.namespace}.*</Export-Package>
						<Private-Package>${bundle.namespace}.*</Private-Package>
						<Bundle-Activator>${bundle.namespace}.CyActivator</Bundle-Activator>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<repositories>
		<repository>
			<id>cytoscape_snapshots</id>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<releases>
				<enabled>false</enabled>
			</releases>
			<name>Cytoscape Snapshots</name>
			<url>https://nrnb-nexus.ucsd.edu/repository/cytoscape_snapshots/</url>
		</repository>
		<repository>
			<id>cytoscape_releases</id>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
			<releases>
				<enabled>true</enabled>
			</releases>
			<name>Cytoscape Releases</name>
			<url>https://nrnb-nexus.ucsd.edu/repository/cytoscape_releases/</url>
		</repository>
		<!--
			Declared only to override the URL scheme.  This module depends on
			org.ops4j.pax.logging:pax-logging-service, whose POM declares a
			repository with this same id over plain http and pulls in
			org.knopflerfish.kf6:log-API.  Maven 3.8.1+ blocks plain-http
			repositories, so dependency collection failed here on any machine
			that had not already cached that artifact.  A repository declared by
			the project wins over one contributed by a dependency's POM, so
			redeclaring the same id over https is enough.  The artifact is not
			mirrored on cytoscape_thirdparty.
		-->
		<repository>
			<id>knopflerfish</id>
			<name>Knopflerfish OSGi Repository</name>
			<url>https://resources.knopflerfish.org/repo/maven2/release</url>
		</repository>
	</repositories>

	<dependencies>
		<dependency>
			<groupId>org.cytoscape</groupId>
			<artifactId>swing-application-api</artifactId>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.ops4j.pax.logging</groupId>
			<artifactId>pax-logging-service</artifactId>
			<version>1.11.2</version>
			<scope>compile</scope>
		</dependency>
	</dependencies>

</project>
